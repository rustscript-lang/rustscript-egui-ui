[package]
name = "rustscript-egui-ui-policy"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"

[dependencies]
egui = "0.35.0"
vm = { package = "pd-vm", path = "../rustscript", default-features = false, features = ["runtime"] }

[dev-dependencies]
pretty_assertions = "1"
